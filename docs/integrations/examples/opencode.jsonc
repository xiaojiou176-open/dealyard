{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "dealyard": {
      "type": "local",
      "enabled": true,
      "command": ["uv", "run", "python", "-m", "dealyard.mcp", "serve", "--transport", "stdio"],
      "environment": {
        "PYTHONPATH": "src"
      }
    }
  }
}
