services:
  postgres:
    image: postgres:16
    container_name: dealwatcherer-postgres
    environment:
      POSTGRES_DB: dealwatcherer
      POSTGRES_USER: dealwatcherer
      POSTGRES_PASSWORD: dealwatcherer
      PGDATA: /data/postgres
    ports:
      - "${POSTGRES_PORT:-15432}:5432"
    volumes:
      - dealwatcherer-postgres:/data/postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dealwatcherer -d dealwatcherer"]
      interval: 5s
      timeout: 5s
      retries: 20

  api:
    build:
      context: .
      target: runtime
    container_name: dealwatcherer-api
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://dealwatcherer:dealwatcherer@postgres:5432/dealwatcherer
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./.runtime-cache:/app/.runtime-cache
    ports:
      - "${API_PORT:-8000}:8000"
    command: ["python", "-m", "dealwatcherer", "server"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3)"]
      interval: 5s
      timeout: 5s
      retries: 20

  worker:
    build:
      context: .
      target: runtime
    container_name: dealwatcherer-worker
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://dealwatcherer:dealwatcherer@postgres:5432/dealwatcherer
    depends_on:
      postgres:
        condition: service_healthy
      api:
        condition: service_healthy
    volumes:
      - ./.runtime-cache:/app/.runtime-cache
    command: ["python", "-m", "dealwatcherer", "worker"]

  frontend:
    image: node:22-bookworm-slim
    container_name: dealwatcherer-frontend
    working_dir: /app/frontend
    environment:
      CI: "true"
    volumes:
      - ./.pnpm-store:/app/.pnpm-store
      - ./frontend:/app/frontend
    ports:
      - "${WEBUI_PORT:-5173}:5173"
    command: ["sh", "-lc", "corepack enable && pnpm install --config.confirmModulesPurge=false && pnpm dev --host 0.0.0.0"]

volumes:
  dealwatcherer-postgres:
